# Local dev only. In production these are Docker secrets (see ../docker-compose.yml) —
# never put real values in a committed .env file.
PORT=3000
DB_HOST=localhost
DB_PORT=5432
DB_USER=vogler
DB_NAME=vogler_maintenance
DB_PASSWORD=devpassword
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=devpassword
JWT_SECRET=dev-only-not-for-production
PUBLIC_APP_URL=http://localhost:5173
SMTP_HOST=smtp.purelymail.com
SMTP_PORT=465
SMTP_USER=maintenance@steinmetz.ltd
SMTP_PASSWORD=get-from-pass-store-clients/steinmetz.ltd/purelymail-maintenance-smtp-app-password
ALERT_EMAIL=scott@steinmetz.ltd
